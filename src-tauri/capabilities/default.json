{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for Spiel — Phase 12 (optional OpenAI provider support)",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "global-shortcut:default",
    "clipboard-manager:default"
  ]
}
