{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Spiel runs entirely locally. The webview only talks to Spiel's own commands; it has no network, filesystem, or shell capability.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "global-shortcut:default"
  ]
}
